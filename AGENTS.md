# Creeper Industry 协作基线

这份 `AGENTS.md` 用来记录对后续开发始终有帮助的背景指导，而不是记录阶段性进度。

## 维护原则

- 这里只写长期有效的信息：项目目标、稳定规则、结构约定、工作流、容易被后续改动误伤的设计边界。
- 不要把容易过期的内容写进来：具体版本号、当前只实现了什么、临时调试方案、近期 TODO、短期平衡数值、一次性的排障结论。
- 如果代码现状与本文档不一致，优先先确认是“文档过期”还是“实现偏离设计”，不要直接把其中一方静默改掉。

## 项目定位

- 这是一个基于 NeoForge 的 Minecraft 模组工程，使用 Gradle Wrapper 构建。
- 项目的核心不是传统“发电-输电-用电”电力模组，而是围绕苦力怕、爆炸、信号工程和高风险工业化展开的“信号工业”。
- 这里的“机器行为”优先考虑被信号驱动，而不是默认套用炉子式燃烧条、能量条或红石替代逻辑。

## 设计基线

### 信号系统

- 信号的核心属性是：幅值、周期、相位、波形。
- 当前系统运行在离散 tick 上。所谓“连续信号”本质仍是离散采样信号，不要按数学连续函数去设计实现。
- 信号分两类：
  - 脉冲信号：典型来源是爆炸，持续 `1 tick`。
  - 连续信号：典型来源是机器，持续存在直到源被移除或参数变化。
- 连续信号的方波规则已经成型：前半周期为 `+A`，后半周期为 `-A`。
- 相位是正式规则，不应被当成可省略的装饰字段；连续信号叠加时需要把相位纳入采样结果。
- 机器读取的是目标位置上的聚合结果，而不是默认逐个读取所有源。

### 传播与聚合

- 普通介质传播按曼哈顿距离衰减，传播 `1` 格损失 `1` 点幅值。
- 普通信号传播当前允许穿过空气和普通方块，传播成本按位置距离计算，而不是按遮挡判定。
- 导管网络提供无损内部传输，但进入导管前和离开导管后仍然要按普通介质计算成本。
- 同一个源到同一个目标只取最佳路径，不应同时累计“空气直达”和“导管绕行”两份贡献。
- 纯连续信号聚合时，结果应来自公共周期上的逐 tick 采样与绝对值平均；混入脉冲信号时，该 tick 按瞬时值结算。

### 导管与接口

- 导管本体不是六面自然漏风的通用信号块。
- 导管只能通过接口与外界交换信号；没有接口的面应视为绝缘。
- 接口是导管方块上的同格挂件，而不是独立方块。
- 已与另一根导管直连的面，不应再安装接口。
- 接口增删、导管放置拆除、导管网络连通性变化，都应视为真实的信号拓扑变化，而不只是渲染变化。

### 更新机制

- 信号系统以变化驱动刷新为主，不应默认退回到“所有接收端每 tick 全量轮询”的方案。
- 连续信号源更新、爆炸脉冲、导管网络变化、接口变化，都会触发受影响接收端的刷新。
- 导管或接口导致的接收端刷新，优先沿用现有“收集受影响目标后统一刷新”的思路，避免散落的重复重算。
- 脉冲、连续源变化和拓扑变化产生的目标应进入同一延迟刷新队列，在世界 tick 末合并去重。
- 连续信号每 tick 只刷新 `continuouslyDrivenTargets` 中的已加载接收端，不允许设备自行恢复信号刷新 ticker。

## 代码结构约定

- `src/main/java/com/secondition/creeperindustry/CreeperIndustry.java` 是模组入口，负责注册方块、物品、方块实体、菜单、数据组件等内容。
- `CreeperIndustryClient.java` 负责客户端菜单与方块实体渲染器注册。
- `content/energy/signal` 是信号系统核心目录，包含信号定义、传播、聚合、接收、刷新、爆炸脉冲与连续信号源逻辑；其 `runtime` 子目录负责运行时组装、访问和生命周期。
- `content/energy/transmission` 是导管与接口目录，负责导管方块、接口安装/拆除和导管连通辅助逻辑。
- `content/production` 和 `content/logistics` 放机器与玩法实体；`foundation` 放轻量的基础方块/菜单骨架。
- `CISignalSourceTypes` 只声明全局只读的信号源类型。世界中的仓库、接收端索引、聚合服务、刷新服务和导管网络管理器统一由当前 `ServerLevel` 的 `SignalRuntime` 持有，并通过 `SignalRuntimeAccess` 访问。

## 多方块实现指导

- 不要把“所有多方块机器都必须像生态瓶一样实现”当成默认前提。
- 只有当机器确实需要让非控制器部分承担交互、容器访问代理、信号接收代理或外部自动化接入时，才优先参考生态瓶这种“控制器集中状态，其余部分转发”的模式。
- 如果采用这种模式：
  - 控制器位置应能从组成块状态稳定反推出。
  - 非控制器部分的菜单打开、物品访问、信号触发等行为应明确转发到控制器。
  - 真正的库存和权威逻辑应保持单点归属，避免多处状态源。
- 生态瓶这类一次生成或拆除完整结构的多方块，不使用只覆盖目标格的客户端预测；客户端等待服务器权威结构更新，避免分批显示与拒绝操作后的幽灵组成块。
- 如果某个多方块不需要这些能力，就不要为了统一风格强行复制生态瓶方案。

## 资源与数据组织

- 正式资源放在 `src/main/resources/assets/creeper_industry/` 下，按 `blockstates`、`models`、`textures`、`lang` 分层。
- `zh_cn.json` 和 `en_us.json` 都已用于名称、界面和调试文本；新增可见文本时应同步维护中英文，避免只补一侧。
- 多方块正式模型资源应与代码尺寸、方块状态拆分保持一致，不要只改模型不改代码尺寸，也不要只改代码尺寸不改资源映射。
- `modeltest/` 更适合放原始 Blockbench 导出或临时中间文件，不要把它当正式资源目录直接接入。
- `Botanical Biosphere` 的模型资源目录可以作为“多方块拆分模型 + `blockstates` 映射”的参考样式：
  - 使用 `base.json`、`empty.json`、`preview.json` 以及 `part_x*_y*_z*.json` 这种组织方式。
  - `blockstates/<block_id>.json` 需要覆盖每个组成块状态到对应子模型的映射。

## 开发时优先参考的文件

- `README.md`：当前项目说明和主要系统概念。
- `docs/design_draft.md`：稳定设计规则、约束和不应轻易回退的取舍。
- `AGENTS.md`：协作时需要长期遵守的项目背景与工作方式。

## 常用命令

```powershell
.\gradlew runClient
.\gradlew runServer
.\gradlew runData
.\gradlew build
```

## 未来代码结构基线

项目当前的 `content/energy`、`content/production`、`content/logistics` 与 `foundation` 分区仍然清楚，可以作为后续扩展基础，不需要为了形式统一进行一次性大规模搬迁。新增系统应优先放入最接近其玩法职责的领域目录；只有当现有目录无法准确表达职责时，才增加新的一级领域。

未来整体结构建议按以下边界发展：

```text
com/secondition/creeperindustry
├─ CreeperIndustry.java                 模组通用入口
├─ CreeperIndustryClient.java           客户端注册入口
├─ CI*.java                             注册表与集中接入类
├─ api/                                 确需跨系统或跨模组使用的稳定接口
├─ client/                              纯客户端界面、渲染与可视化
├─ content/
│  ├─ creeper/                          苦力怕捕获、繁殖、性状、改造与变种
│  ├─ explosion/                        爆炸工程、防爆等级与爆炸功能设备
│  ├─ energy/
│  │  ├─ signal/                        信号定义、运行时、传播、聚合与刷新
│  │  └─ transmission/                  导管、接口与传输拓扑
│  ├─ automation/                       传感、执行、收集、破坏、投放与机械控制等实现基础功能的功能性设备
│  ├─ production/                       输入脉冲和材料就直接实现产出某物品的目的性设备
│  │  ├─ biosphere/                     三类生态瓶
│  │  └─ printer/                       蓝图打印与结构施工设备
│  ├─ blueprint/                        蓝图扫描、文件格式、校验、变换与放置计划
│  ├─ logistics/
│  │  ├─ storage/                       储物唱片与批次存储
│  │  ├─ rocket/                        制导烟花与无线运输
│  │  ├─ launcher/                      发射与接收设施
│  │  └─ dropper/                       精准投放与玩家交通
│  └─ endgame/fission/                  “怕裂变”容器、阵列与控制设备
├─ data/                                配方、条件、序列化器与数据重载逻辑
├─ foundation/                          与具体玩法无关的轻量通用实现
└─ integration/                         可选模组兼容层，按目标 modid 分包
```

这是一份职责地图，不要求提前创建空目录，也不要求把单个小功能拆成多层包。只有当某个目录下已经出现多个相互独立的职责时，再继续按 `source`、`receiver`、`recipe`、`runtime`、`network` 等子职责拆分。

### 根包与注册类

- `CreeperIndustry` 只负责装配注册表、事件监听器和通用启动流程，不放置机器行为或玩法规则。
- `CreeperIndustryClient` 只负责客户端注册和客户端事件接入，不持有服务器权威状态。
- `CIBlocks`、`CIItems`、`CIBlockEntityTypes`、`CIEntityTypes`、`CIMenuTypes`、`CIDataComponents`、`CIRecipeTypes` 等类只声明注册项和必要构造关系。
- 注册类开始过长时，允许在对应领域内建立注册子类，再由根注册类集中调用；不要让内容实现反向依赖多个平行注册入口。
- 注册 id、资源路径、网络载荷 id 和数据组件 id 一律使用小写 `snake_case`，并通过 `CreeperIndustry.asResource` 统一创建本模组资源位置。
- `CISignalSourceTypes` 只负责类型声明；任何需要世界状态的代码必须通过 `SignalRuntimeAccess.get(level)` 获取当前维度运行时，不得重新向该类加入静态仓库或服务入口。

### 通用端与客户端隔离

- 含有 `net.minecraft.client`、渲染器、Screen、客户端输入或 HUD 逻辑的类统一放在 `client/` 下，并按通用端领域镜像组织。
- 通用端方块、方块实体、实体、菜单、配方和数据类不得 import 客户端类。
- Screen 与 Renderer 只能由客户端入口引用；专用服务器加载路径不得触及客户端类型。
- 菜单负责服务器权威的槽位与数据同步，Screen 只负责显示和发送用户操作，不在客户端直接决定生产结果或修改权威库存。
- 现有与内容类同包的 Screen/Renderer 可以在相关系统下次实质修改时迁移，不为单纯整理目录进行大范围改名。

### 领域对象职责

- `Block` 主要负责方块状态、放置/拆除、交互入口、形状和 ticker 选择，不承担复杂生产流程。
- `BlockEntity` 负责持久状态、库存、同步和本地流程协调；复杂配方匹配、遗传计算、蓝图校验、信号路由和网络搜索应委托给独立服务。
- `Menu` 不复制方块实体业务规则，只调用权威对象暴露的操作。
- `Entity` 的服务器状态、需要同步的客户端表现和持久数据需要明确分开；不能依赖仅客户端存在的字段完成权威行为。
- Event Handler 只负责把 NeoForge/Minecraft 事件转换为领域调用，不在事件类中堆积完整业务系统。
- 单个类如果同时管理库存、配方表、信号接收、生产结算、菜单文本和多方块代理，应在继续增加职责前拆出配方解析器、生产策略或控制器服务。现有 `BiosphereBlockEntity` 是原型参考，不应继续把三类生态瓶的新配方和新行为都硬编码进同一个类。

## 信号运行时结构规范

- 信号定义、传播成本、聚合算法、接收端索引、导管拓扑和刷新调度保持可分离职责，不把所有算法合并进方块实体。
- 信号运行时状态通过非持久化 Level Attachment 按 `ServerLevel` 隔离。仓库、接收端索引、刷新队列和导管缓存均属于单个 `SignalRuntime`。
- 不新增无法在世界卸载、服务器停止或维度卸载时清理的静态可变状态；新增运行时服务需要由 `SignalRuntime` 组装，并在 `close()` 中清理自身状态。
- 信号源和接收端的注册、更新、移除必须成对设计；方块拆除、区块卸载、维度卸载和服务器停止都不能留下失效位置。
- 连续信号的 tick 采样不等于允许所有接收端各自进行全局扫描。需要刷新连续波形时，应由统一调度服务只更新受影响接收端。
- 接收端在加载时注册，在拆除或区块卸载时注销；连续信号源的注册、更新和移除同样必须通过当前运行时成对完成。
- 同一 tick 内批量加载接收端时只能标记连续活动目标集合失效，并在集中刷新前统一重建一次；不得为多方块的每个组成块重复执行全量重建。
- 生态瓶、信号检测器和后续接收设备都不设置独立信号 ticker；接收设备只实现自身响应，不复制调度逻辑。
- 新机器不得通过每 tick 遍历全部信号源、全部接收端或全部导管来获得结果。
- 导管拓扑缓存、路径成本计算和受影响目标收集应保持独立。`DuctNetworkManager` 若继续增长，优先按“拓扑缓存”“网络发现”“路径/成本查询”“变更影响收集”拆分。
- 信号系统的纯计算部分尽量只依赖不可变值对象，便于验证幅值、周期、相位、叠加与最佳路径规则。

## 数据驱动与配方规范

- 正式生产内容不得继续以 `Map<Item, ItemStack>` 或成组 `Items.*` 判断作为主要配方来源。
- 植物、动物、怪物生态瓶、3D 打印、信号转换、苦力怕改造和防爆材料加工应使用数据包可重载的配方或独立数据类型。
- 输入匹配优先使用 `Ingredient`、物品标签、方块标签、实体类型标签或明确的数据组件条件，避免只接受某一个硬编码物品。
- 配方输出使用注册表 id 和 Codec/StreamCodec 可序列化数据表达，不保存运行时数字 id。
- 信号条件作为配方的一部分时，应使用独立结构表达幅值范围、周期、瞬时值符号、边沿、持续时间或脉冲要求，不为每台机器复制一套判断代码。
- 机器只依赖通用配方接口；具体配方通过 Recipe Manager 或数据重载后的索引查询。
- 数据重载后需要失效的缓存必须统一刷新，不能继续引用旧配方对象。
- 原型期硬编码表可以暂时保留，但新增正式内容时应优先建立数据驱动入口，再迁移相应原型数据。
- 三类生态瓶统一使用 `biosphere_cultivation` 配方；不要重新把模板、消耗品、产物或基础信号条件硬编码回 `BiosphereBlockEntity`。
- 生态瓶的信号门槛必须来自当前模板对应的配方，不得按同类型任意配方判断；消耗品不足只阻止结算，不得重置信号上升沿状态。

## 苦力怕培育代码边界

- 苦力怕的基础性状、改造机会、遗传信息和变种身份使用可序列化的数据对象表达，不散落为多个互不关联的布尔字段。
- “每只苦力怕只有一次定向概率改造机会”是权威规则，改造状态必须随实体保存并正确同步必要表现。
- 改造材料只声明候选性状、权重和限制；概率抽取与遗传计算由独立服务负责。
- 繁殖时先根据父母性状生成后代继承结果，再给予后代自己的一次改造机会，不直接修改父母数据。
- 性状 id 应使用资源位置，允许数据包增加或调整改造配置，也为未来兼容其他模组实体或物品保留空间。
- 客户端只接收展示所需的性状摘要，完整改造与遗传判定始终在服务器端完成。

## 蓝图与结构施工规范

- 蓝图文件格式、世界内蓝图物品和 3D 打印任务分成三层：文件负责分享，物品负责引用或携带选择，打印任务负责服务器内施工状态。
- 蓝图保存注册表资源位置、方块状态属性和经过白名单处理的附加数据，不保存运行时注册表数字 id。
- 蓝图格式从第一版开始携带 `format_version`、尺寸、作者/说明等元数据和依赖列表，为跨版本迁移保留入口。
- 导入蓝图时必须在服务器端重新校验体积、坐标、方块白名单、依赖、NBT/组件大小和玩家权限；不得信任客户端文件内容。
- 方块实体数据默认按最小必要字段保存。库存、实体、生物、不可获取方块和可产生复制收益的数据是否允许记录，按 `docs/future_roadmap.md` 的待定边界逐项确认后实现。
- 扫描、旋转、镜像、材料统计、放置顺序和实际世界写入分别由独立步骤处理，避免一个打印机方块实体同时解析文件并直接执行全部操作。
- 打印过程必须可暂停、恢复和失败报告；大结构按预算分 tick 施工，不在单 tick 内批量放置全部方块。
- 跨模组方块缺失时提供清晰的缺失依赖列表，不静默替换为空气，也不因单个未知 id 使整个存档无法加载。
- 兼容方块的特殊数据需要显式适配器；不得无过滤复制任意方块实体 NBT。

## 自动化、库存与跨模组兼容

- 原版漏斗兼容可以通过 `WorldlyContainer` 提供，但正式机器还应在需要时通过 NeoForge capability 暴露物品、流体或其他能力。
- capability 必须按面声明允许的输入输出，不直接把完整内部库存无条件暴露给外部模组。
- 多方块机器的 capability 统一转发到权威控制器，不能让每个组成块拥有独立库存副本。
- 物品过滤优先使用标签、Ingredient 或可扩展谓词；不要用大量 `stack.is(具体物品)` 作为长期兼容方案。
- 与其他模组的可选兼容代码放入 `integration/<modid>/`，通过加载检查与可选依赖隔离。通用内容类不能直接引用一个未声明为必需依赖的模组类。
- 优先使用 NeoForge 事件、capability、数据组件和注册表 API；只有公共 API 无法实现目标时才使用 Mixin。
- 每个 Mixin 应只处理一个明确问题，记录目标、原因和兼容风险；不要把空 Mixin 配置当成未来功能的默认入口无限保留。
- 不通过覆盖原版或其他模组的完整方法实现兼容，尽量使用事件、注入点最小的补丁或独立适配层。
- 对外提供 API 之前先确认确实存在跨系统或跨模组调用者；不要为单个内部实现提前建立庞大公共抽象。

## 网络与安全规范

- 每种网络载荷使用独立 payload 类型、明确的 `ResourceLocation` id 和对应 StreamCodec。
- 客户端发往服务器的数据只表达玩家意图，不能直接声明“生产完成”“物品已经获得”或其他权威结果。
- 服务端处理网络请求时至少校验发送者、维度、目标是否加载、交互距离、菜单/设备归属、权限、数值范围和请求频率。
- 坐标、蓝图体积、字符串、集合和文件相关数据必须设置上限，避免畸形数据造成内存、磁盘或区块加载问题。
- 世界修改、实体操作和库存修改在正确的服务器线程执行。
- 网络协议版本变化应有明确兼容策略；开发期可以拒绝不匹配版本，但不能静默按错误格式解析。
- 空处理器只适用于尚未接入的占位载荷，正式功能合入前必须完成权威校验和实际处理，或移除无效注册。

## 持久化与同步规范

- 方块实体、实体、物品数据组件和世界级数据分别使用适合其生命周期的持久化方式，不用单一全局 Map 代替世界数据。
- 只持久化无法从世界状态重新推导的权威数据；导管连通缓存、临时聚合结果和界面显示缓存通常不应写入存档。
- 保存与读取需要容忍字段缺失，并为格式升级保留默认值或迁移路径。
- 所有 ItemStack、注册表对象和跨模组引用使用 HolderLookup、Codec 或资源位置正确序列化。
- 客户端同步采用最小必要数据。大型蓝图、完整网络拓扑和全量配方数据不应通过普通方块实体更新包反复发送。
- 玩家临时状态（例如精准投放过程中的伤害保护）必须有明确开始、结束和异常清理条件，不能依赖永久布尔标记。

## 资源目录规范

- 客户端资源继续放在 `assets/creeper_industry/`，服务器数据放在 `data/creeper_industry/`，两者不得混放。
- `assets` 下按 `blockstates`、`models`、`textures`、`lang`、`sounds` 等原版目录组织。
- `data` 下按 `recipe`、`loot_table`、`tags`、`advancement` 以及项目自定义数据目录组织；具体目录名遵循当前 Minecraft/NeoForge 版本约定。
- 注册正式方块和物品时，同一功能提交应同步考虑模型、方块状态、战利品表、合成/加工配方、标签和中英文文本；尚未完成资源的占位内容需要明确保持为原型。
- 可以由数据生成稳定产出的普通模型、标签、配方和战利品表优先使用 Datagen；手工维护大型多方块切片模型仍按现有模型工作流处理。
- `src/generated/resources` 只存放可重复生成的结果，不手工编辑生成文件。
- 通用材质名不得使用 `texture.png` 这类无法表达用途的名称；新增资源使用能对应方块、部件或用途的稳定命名。
- 中英文翻译键必须一致；代码中不硬编码面向玩家的文本。

## 命名与代码风格

- Java 包名全部使用小写。现有 `foundation/blockEntity` 后续实质修改时迁移为 `foundation/blockentity`，不为单纯改大小写制造跨平台 Git 噪音。
- 类名使用 `UpperCamelCase`，方法与字段使用 `lowerCamelCase`，常量使用 `UPPER_SNAKE_CASE`。
- 同一概念保持统一术语，例如信号使用 `period` 表示周期，不在不同数据组件、界面和业务类中混用 `frequency` 与 `period` 而没有明确换算语义。
- 布尔变量使用能够表达状态或条件的名称，避免 `flag1`、`enabled2` 等不可维护命名。
- 面向注册表和数据文件的类型优先使用小型不可变 record/value object；可变世界状态保留在实体、方块实体或运行时容器中。
- 公共方法需要维持明确的不变量。若参数只允许偶数周期、非负数量或已加载位置，应在数据构造边界统一验证，而不是由每个调用者猜测。
- 不因为未来可能需要而提前引入通用框架；第一次出现的单一逻辑保持局部，确认第二个真实使用者后再抽取共享实现。

## 性能与更新原则

- 优先变化驱动、索引查询和局部重算，避免每 tick 扫描全世界方块、实体、信号源或接收端。
- 必须 tick 的设备应先执行廉价退出条件，并只在服务端执行权威逻辑。
- 大型蓝图施工、导管网络重建、范围收集和阵列计算需要分批或缓存，避免单 tick 完成长时间操作。
- 缓存必须有明确失效条件；如果无法完整列出失效条件，先使用正确的局部计算，不引入可能返回旧结果的缓存。
- 区块未加载时不为普通设备强制加载远处区块。无线物流、蓝图施工和精准投放如果需要区块加载，应使用明确、有限且可清理的加载策略。
- 性能优化不能改变信号最佳路径、聚合、相位、导管接口或多方块权威控制器等既定语义。

## 验证边界

- 信号采样、聚合、传播成本、遗传计算、蓝图旋转/镜像和材料统计尽量写成可独立验证的纯逻辑。
- 世界交互规则适合使用 GameTest 或小范围集成验证时，应覆盖区块卸载、方块拆除、维度切换和多人同时操作等生命周期场景。
- 网络功能至少验证非法坐标、超距操作、错误菜单、缺失目标、过大载荷和重复请求不会修改服务器状态。
- 数据驱动内容至少验证数据重载、缺失依赖和错误资源 id 能给出可诊断结果。
- 协作开发中仍遵守本文前部约定：不要擅自启动客户端、服务器或自动化动态测试；是否运行相应验证由当前任务范围和用户指令决定。

## 当前结构的演进原则

- 当前项目属于可继续扩展的原型结构，不进行一次性“为了规范而规范”的全仓重构。
- 修复或实现某个系统时，只迁移与该任务直接相关、且会阻碍继续实现的职责。
- 客户端类位置、空数据生成入口和占位网络处理器属于后续正式化时优先处理的结构债务。
- 占位方块、菜单和实体可以保留，但在接入正式玩法时必须同时补齐权威逻辑、数据、资源、同步与兼容边界。
- `docs/future_roadmap.md` 决定长期内容目标；本文件决定实现这些目标时必须遵守的稳定结构和工程边界。两者发生冲突时先讨论并同步修订，不静默选择其中一方。
